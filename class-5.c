//write a c program to check weather the given input is even or odd using conditional operator?

//find the largest number among the three inputs
//Give a choice to perform the arithematic operations take two inputs for number case should be the operators?
//Write a program to get second largest number among the three number?

#include<stdio.h>
int main(){
	int a;
	printf("Enter a number: ");
	scanf("%d",a);
	if(a%2==0)
		printf("Even");
	else
		printf("odd");	
}
